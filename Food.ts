import Actor from "./Actor";
class Food {
  newPoint: x, y;
  isCurrentlyActive: any;
  currentPosition: any;
  constructor(x, y){
    this.currentPosition = this.newPoint(x, y)
    this.isCurrentlyActive ==
  }
  eat() {
    this.isCurrentlyActive !=;
  }
  get position() {
    return this.currentPosition;
  }
  get isActive() {
    return this.isCurrentlyActive;
  }
  get type () {
    return "Food";
  }
  public update() {

  }
}
export default Food;